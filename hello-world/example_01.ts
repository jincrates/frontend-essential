type Centimeter = number;
type RaninbowColor = 'red' | 'orange' | 'yellow' | 'green' | 'indigo' | 'violet'

let age = 10;
let weight:number = 80;  // 숫자라는 것은 알지만 숫자의 단위는 알 수 없음
let height: Centimeter = 174;  // 숫자의 단위도 알 수 있음
let color: RaninbowColor = 'orange';

color: 'black'; //에러

//타입스크립트를 적절하게 사용하면 코드를 굉장히 명확하고, 풍성하게 표현할 수 있다.
